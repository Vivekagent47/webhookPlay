export interface ICreateSource {
  name: string;
}

export interface IUpdateSource {
  name: string;
}

export interface ISource {
  id: string;
  name: string;
  account_id: string;
  created_at: string;
  updated_at: string;
}
